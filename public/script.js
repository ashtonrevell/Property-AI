document.querySelectorAll('#year').forEach(el=>el.textContent=new Date().getFullYear());const messages=document.getElementById('messages');const form=document.getElementById('chatForm');const input=document.getElementById('chatInput');function addMessage(text,type='ai'){if(!messages)return;const div=document.createElement('div');div.className=`bubble ${type}`;div.textContent=text;messages.appendChild(div);messages.scrollTop=messages.scrollHeight;}async function askAI(question){addMessage(question,'user');addMessage('Thinking…','ai');try{const response=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:question})});const data=await response.json();messages.lastChild.remove();addMessage(data.answer||'No response received.','ai');}catch(error){messages.lastChild.remove();addMessage('The AI assistant is unavailable right now.','error');}}if(form&&input){form.addEventListener('submit',e=>{e.preventDefault();const q=input.value.trim();if(!q)return;input.value='';askAI(q);});}document.querySelectorAll('[data-question]').forEach(btn=>btn.addEventListener('click',()=>askAI(btn.getAttribute('data-question'))));